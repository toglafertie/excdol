export const selectSigningError = (state: NativeSendRootState) => {
  // Replace this with the actual logic to select the signing error from the state
  return state.signingError; // Example: accessing a specific field from the state
};
